apiVersion: v1
kind: Service
metadata:
  name: jfieuir
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "jfieuir"
    port: 8080
  selector:
    app: jfieuir
